__pycache__/
*.pyc
.venv/
venv/
.env
.DS_Store

# Run artifacts — regenerate with `python run.py --mock`.
runs/*
!runs/.gitkeep
# ...except the committed sample run (real engine output, shown by default in the viewer).
!runs/demo-set-cover/
!runs/demo-set-cover/**
