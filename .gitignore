__pycache__/
*.pyc
.venv/
venv/
.env
.DS_Store

# Run artifacts — regenerate with `python run.py --mock`.
runs/*
!runs/.gitkeep
# ...except the committed sample runs (real engine output, shown in the viewer's run-picker).
!runs/demo-*/
!runs/demo-*/**
